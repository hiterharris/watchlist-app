import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Watchlist.hiterharris.app',
  appName: 'watchlist-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
